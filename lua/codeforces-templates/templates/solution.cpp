#include <iostream>
#include <bits/stdc++.h>

using namespace std;
using vi = vector<int>;

int solve() {
    int n;
    cin >> n;

    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    cin >> t;
    while (t--) {
        int result = solve();
        cout << result;
    }
}
